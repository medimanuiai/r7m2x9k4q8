# Jyothishyam

This repository contains the Jyothishyam project.

Quick steps to push from this machine:

1. Add the generated SSH public key to your GitHub account (Settings → SSH and GPG keys).
2. Run `git push -u origin pilot` after the key is added.

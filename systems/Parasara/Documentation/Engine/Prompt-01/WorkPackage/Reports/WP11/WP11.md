# WP11 — Remaining Canonical Predicate Migration Completion Report

Date: 2026-07-16

**VERDICT: COMPLETE**

**WP12_READY: YES**

## 1. Model and reasoning used

Implementation used the GPT-5-based Codex model exposed by this session; no separate reasoning-tier identifier was surfaced. Work was repository-first and tests-first: reproduce every hard prerequisite, read the locked decisions/audits and WP02–WP10 evidence, add a focused failing WP11 suite, implement the smallest explicit prepared-state handlers/dispatch change, then run the complete dual-lane matrix.

## 2. Prerequisites, baseline, and inherited tree

Exact completion evidence was found at `WorkPackage/WP00-R/WP00-R.md`, `Reports/WP01-P0-Characterization-Fixtures-and-Tests.md`, and `WorkPackage/WP02/WP02.md` through `WorkPackage/WP10/WP10.md`. Every predecessor is complete; WP10 records `VERDICT: COMPLETE` and `WP11_READY: YES`. WP09 still has an instance-owned default-256 bounded LRU that stores matched/unmatched only. WP10 still owns an explicit `ConditionEvaluator` with typed recursive results and deterministic short-circuiting.

The locked execution plan, Final Audit Consolidation, required Audit 01, 02, 04, 05, 07–12, 15, and 17–22 inventories, and predecessor reports were used as architecture/compatibility evidence. The inherited dirty tree (accumulated WP00-R–WP10 changes and prompt moves) was recorded and preserved; no unrelated file was restored or claimed.

Pre-edit reproduction in Python 3.14.6 and 3.11.9:

| Gate | 3.14 | 3.11 |
|---|---:|---:|
| collection | 651 nodes | 651 identical nodes |
| complete run 1 | 649 passed, 2 skipped | 649 passed, 2 skipped |
| complete run 2 | 649 passed, 2 skipped | 649 passed, 2 skipped |
| Yoga normal/reverse/A/B/C | 5 passed each | 5 passed each |
| both loader orders | 2 passed each | 2 passed each |
| lint | five files once | same |
| strict snapshots 1/2 | match | match |

No production or test edit preceded this green hard gate.

## 3. Files and APIs changed

WP11 changed only:

- `systems/Parasara/engine/rules/canonical_predicates.py` — new explicit canonical handlers and read-only dispatch;
- `systems/Parasara/engine/rules/evaluator.py` — five-definition routing and relevant-context projection;
- `tests/rules/test_remaining_canonical_predicates.py` — 31 focused WP11 contracts;
- `tests/rules/test_predicate_evaluator_cache.py` — replaced the now-obsolete “unmigrated” assertions with canonical route/cache assertions;
- `tests/rules/test_condition_evaluator.py` — replaced the now-obsolete unmigrated-leaf assertion;
- this report.

New handler APIs:

```python
evaluate_house_occupant(parameters, state, context) -> PredicateResult
evaluate_aspect_exists(parameters, state, context) -> PredicateResult
evaluate_functional_role(parameters, state, context) -> PredicateResult
evaluate_planet_exalted(parameters, state, context) -> PredicateResult
CANONICAL_HANDLER_DISPATCH  # read-only MappingProxyType
```

The existing `evaluate_planet_in_house` remains the fifth handler. Legacy registry handlers and active compatibility callers were not changed.

## 4. Six-ID/five-definition identity table

| Exposed ID | Canonical result/cache ID | Version | Canonical handler |
|---|---|---|---|
| `ASPECT` | `ASPECT_EXISTS` | 1.0.0 | `evaluate_aspect_exists` |
| `ASPECT_EXISTS` | `ASPECT_EXISTS` | 1.0.0 | `evaluate_aspect_exists` |
| `FUNCTIONAL_ROLE` | same | 1.0.0 | `evaluate_functional_role` |
| `HOUSE_OCCUPANT` | same | 1.0.0 | `evaluate_house_occupant` |
| `PLANET_EXALTED` | same | 1.0.0 | `evaluate_planet_exalted` |
| `PLANET_IN_HOUSE` | same | 1.0.0 | existing `evaluate_planet_in_house` |

Registry inventory remains six exposed IDs, five definitions, and five unchanged legacy handler identities. `HOUSE_OCCUPANT` remains separate from `PLANET_IN_HOUSE` in schema order, identity, trace, evidence, definition fingerprint, and cache key.

## 5. Parameters, capabilities, and relevant context

| Definition | Exact normalized parameters | Required capabilities in order | WP09 relevant context |
|---|---|---|---|
| `HOUSE_OCCUPANT` | `house`, `planet` | `planets.house_placement`, `planets.normalized` | none |
| `ASPECT_EXISTS` | optional `from_house`, `to_house`, `from_planet`, `to_planet` | `aspects.whole_sign_graph` | none |
| `FUNCTIONAL_ROLE` | nonempty `role_in` tuple | `chart.lagna`, `planets.normalized`, `roles.functional` | `selected_planets` only |
| `PLANET_EXALTED` | `planet` | `dignity.exaltation_facts`, `planets.normalized` | none |
| `PLANET_IN_HOUSE` | `planet`, `house` | unchanged | none |

Every invalid mapping returns `invalid_parameters` before capability/fact access. The WP05 schemas/vocabularies and WP06 declarations were not changed.

## 6. Truth and availability matrices

### HOUSE_OCCUPANT

| Observation | Status |
|---|---|
| entity present, actual house equals expected | matched |
| entity present, valid actual house differs | unmatched |
| requested planet absent | missing_capability / `missing_planet_entity` |
| house fact absent | missing_capability / `missing_occupant_house` |
| required capability unavailable | missing_capability with shared capability code |

### ASPECT_EXISTS

| Observation | Status |
|---|---|
| any preserved edge satisfies every supplied filter | matched |
| ready nonempty graph, no same-edge match | unmatched |
| ready-empty graph | unmatched |
| graph missing/malformed/version-mismatched/unsupported | missing_capability |

### FUNCTIONAL_ROLE

| Observation | Status |
|---|---|
| at least one candidate has an exact admitted role | matched (decisive even with other unavailable candidates) |
| no match, every requested entity/fact present | unmatched |
| explicit empty selection | unmatched with zero candidates |
| no match plus absent entity/role fact | missing_capability |
| ready-empty role map with present selected/all-present candidates | missing_capability |

All candidates are evaluated; all matches are preserved in canonical nine-planet order.

### PLANET_EXALTED

| Prepared record | Status/meaning |
|---|---|
| one `legacy_planet_flags` Boolean `True` | matched / `explicit_flag_boolean` |
| one `legacy_planet_flags` Boolean `False` | unmatched / `explicit_flag_boolean` |
| one `legacy_metadata_exaltations` finite numeric, including zero | matched / `configured_metadata_entry` |
| absent planet or absent fact | missing_capability |
| malformed/conflicting capability | missing_capability / `malformed_capability` |
| any other safe source/value or multiple-source combination | missing_capability / `unsupported_exaltation_source` |

No sign, longitude, degree, orb, dignity, or source preference is evaluated.

## 7. Exact evidence schemas

- House factual evidence: `requested_occupant`, `planet`, `expected_house`, `actual_house`, `equal`, `capabilities`. Availability variants replace actual/equality with `entity_state` and optional `fact_state`.
- Aspect evidence: `filters`, `capability`, `graph_available`, `edge_count`, `matched_indexes`, `matched_edges`. Edge projections preserve `source`, `target`, optional `aspect`/`kind`, order, and duplicates; matched house-filter projections additionally carry observed `from_house`/`to_house`.
- Functional-role evidence: `expected_roles`, `selection_policy`, `selected_planets`, `capabilities`, `candidates`, `matched_planets`. Candidate rows contain `planet`, `entity_state`, `fact_state`, `matches_expected`, optional `observed_role`, and bounded issue tuples when unavailable.
- Exaltation evidence: `planet`, `capabilities`, `entity_state`, `fact_state`, `records`, `interpretation_rule`, `factual_state`. Records contain exactly `source_kind` and preserved Boolean/numeric `value`.

Capability rows contain exactly `capability_id`, `expected_version`, `observed_version`, `readiness`, `source_kind`, `content_empty`, and safe `issues`. No raw envelope, graph `by_planet`, diagnostics, provider object, path, score, traceback, exception string, time, or cache telemetry is included.

## 8. Trace and error vocabularies

Trace paths are deterministic and only executed steps are emitted:

- House: `house_occupant.parameters`, `.capabilities`, `.planet`, `.house`, `.result`;
- Aspect: `aspect_exists.parameters`, `.capabilities`, `.graph`, `.comparison`, `.result`;
- Role: `functional_role.parameters`, `.capabilities`, `.candidates`, `.comparison`, `.result`;
- Exaltation: `planet_exalted.parameters`, `.capabilities`, `.planet`, `.fact`, `.interpretation`, `.result`.

Shared errors are `invalid_parameters`, `missing_capability`, `malformed_capability`, `capability_version_mismatch`, `unsupported_capability`, `ready_empty_not_allowed`, and safe `predicate_execution_error`. Predicate errors are `missing_planet_entity`, `missing_occupant_house`, `missing_aspect_graph_fact`, `missing_functional_role_entity`, `missing_functional_role_fact`, `missing_exaltation_fact`, and `unsupported_exaltation_source`. Messages are fixed; details are bounded/canonical; unexpected exception text/type/path/traceback never escapes.

## 9. Aspect preservation decisions

Only `aspects.whole_sign_graph@1.0.0` is inspected. No basic-list conversion, inference, reversal, sort, or deduplication occurs. Every filter must match the same edge. Empty filters match any nonempty graph. Ready-empty is factual unmatched. Edges are examined in source sequence and duplicate matches remain duplicated with their original indexes.

Absent optional edge fields remain absent in evidence. `target=None` matches empty filters, cannot satisfy a concrete `to_planet`, and cannot satisfy `to_house`; this is the characterized legacy comparison behavior. Graph `by_planet`, diagnostics, and raw envelopes are excluded.

## 10. Functional-role policy and no-producer proof

`selected_planets=None` means all present prepared planets; `()` means explicitly none; nonempty selections are WP07-canonicalized. Only `selected_planets` enters the relevant context hash. `evaluation_instant` is neutral. `system_scope` is checked by the evaluator and `evaluation_mode` remains the fixed default.

The handler imports only prepared-state query APIs. Static tests reject `compute_functional_roles`, filesystem/CWD, time, random, UUID, reflection/import scanning, and raw producer access. It never reads a role table or executes a producer.

## 11. Exaltation preservation and no-doctrine proof

The handler observes WP07 source-discriminated records only and accepts exactly the two locked source/value shapes. Numeric metadata is treated as configured-entry presence, not current longitude. Unknown or multi-source safe combinations fail non-factually. Static and semantic tests prove no sign/degree/orb comparison, raw flag/metadata read, or source precedence exists in the handler.

## 12. WP09 evaluator/cache integration

The former single-handler branch is now an explicit five-definition route: the existing PLANET_IN_HOUSE handler plus a read-only four-entry table. There is no reflection, scan, mutable registration, or legacy fallback.

- Alias resolution precedes routing/key construction.
- Equivalent `ASPECT` then `ASPECT_EXISTS`: cold/warm, one entry, key SHA-256 `c942fbc11ab710d7b880a90674c73c66e32286e9edab8d595b7710d53d317a38`.
- Functional role Sun/Moon selections: two entries with context hashes `78a084a59fc5e3532e06158627314e15c67f54047b12de3ab64ad1e158d5c43b` and `561b35fbfae5600cc9ef57040d7f3528b34597019754be9daf0a1388a52e99ca`; key hashes `5c19d63e2b9acc03a580080fb8b3e1e7b630ed8e023681e6f77eaa6893b7f4d9` and `dec37bf2518de0e92b2981829aa119b37f8c9074222c6ee9b46cb62e2cacbb98`.
- Equivalent HOUSE_OCCUPANT and PLANET_IN_HOUSE facts retain two entries.
- Only matched/unmatched store; invalid/missing/error re-evaluate.
- Existing capacity/LRU/freeze/clear/isolation/concurrency tests remain green.

## 13. WP10 condition integration

All six exposed IDs now evaluate as canonical leaves; aliases return `ASPECT_EXISTS`. A six-leaf AND tree retains six complete child results, one shared Aspect cache entry, five total definition/parameter entries, and matched parent status. Its logical payload is 17,121 bytes, SHA-256 `f12996360f946a50be485422e1531525b4a8958f51ce90d07da8cde5853b100d` in both lanes. Unknown IDs and `HOUSE_LORDS_COMBINATION` remain typed `unknown_condition_type` errors. WP10 parent ordering, short-circuit, skips, and recursive serialization were not changed.

## 14. Exact representative logical/full bytes and hashes

Fresh Python 3.14 and 3.11 processes produced identical values:

| Representative | Logical bytes / SHA-256 | Full bytes / SHA-256 |
|---|---|---|
| HOUSE_OCCUPANT matched | 2,215 / `027b92af98aa3e79ac7af6a735a2fba2bd291b77e20dee9ee535a79ff0bf2db9` | 2,259 / `1546d328331c664e8dc5121b1c5b59edb50d5ec78dc075adba768855a36804bc` |
| ASPECT_EXISTS matched | 1,922 / `b950f9413e5fea809b03d13c660263204c1ab23cb175f2a964e6c166e71c68e3` | 1,966 / `657c10947c620396b91d1391a6f3c5ae8476c503a94b912f2ec4c95946d4d38b` |
| FUNCTIONAL_ROLE matched | 2,889 / `e17d85b154cb4aa60e7ea0d9096910ef7280bd3bb90bf0430eeaeb6e8193c7f2` | 2,933 / `eb50fbafbf3d517cd955e7a6db09f702d36d5ee33152c2895ebdcee28757d87f` |
| PLANET_EXALTED matched | 2,620 / `d9e397f2004374384be092cadce7d6045596bbed84f4c746a3f5c5d747753aa3` | 2,664 / `e1fdf5c7ec3d06c7f19e935994eb8434096a6ac20d13b8d4051815d14c780687` |
| mixed six-leaf condition | 17,121 / `f12996360f946a50be485422e1531525b4a8958f51ce90d07da8cde5853b100d` | telemetry-bearing; logical identity is the locked cross-process contract |

Tests also prove full projection differs only by telemetry and logical equality/bytes remain cache-warmth neutral.

## 15. Test traceability

`test_remaining_canonical_predicates.py` covers inventory/dispatch, invalid-before-query behavior, per-predicate truth and availability, every role vocabulary value, selected-context identity, Aspect alias/order/duplicates/empty/target-none, exaltation source/value policy, unexpected-error safety, exact bytes/hashes, deep immutability, static purity, six-ID leaves, and the mixed tree. Existing WP05–WP10 suites cover complete schema matrices, readiness/version/source/digest isolation, bounded cache concurrency, recursive serialization, and condition precedence. WP01 characterization/full suites cover unchanged legacy/Yoga/Career/public behavior.

Tests were added before production: the first meaningful red run failed at collection because `canonical_predicates` did not exist. No test was deleted or weakened; two obsolete “not migrated” assertions were advanced to WP11 canonical expectations.

## 16. Exact dual-lane validation

All pytest commands used `PYTHONDONTWRITEBYTECODE=1`, `PYTHONPATH=.`, `-o addopts=`, `-p no:cacheprovider`, unique ignored base temps, and `NDASTRO_USE_SRTM=0` where full/snapshot behavior applies.

Representative commands:

```powershell
& <python> -m pytest -q -o addopts= -p no:cacheprovider --basetemp <unique> <WP11 focus>
& <python> -m pytest -q -o addopts= -p no:cacheprovider --basetemp <unique> <WP02-WP10 focus>
& <python> -m pytest -q -o addopts= -p no:cacheprovider --basetemp <unique> tests/rules
& <python> -m pytest -q -o addopts= -p no:cacheprovider --basetemp <unique> tests/rules/test_registered_predicates_characterization.py tests/enrichments/test_yoga_characterization.py systems/Parasara/tests/test_career_characterization.py
& <python> -m pytest --collect-only -q -o addopts= -p no:cacheprovider
& <python> -m pytest -q -o addopts= -p no:cacheprovider --basetemp <unique>
& <python> tools/rules_lint.py systems/Parasara/rules/parashara/v1
& <python> systems/Parasara/tools/ci_snapshot_check.py --fixture systems/Parasara/fixtures/golden_chart_01.json --approved systems/Parasara/tests/snapshots/output_golden_chart_01.json --out <ignored-temp>
```

| Final gate | Python 3.14.6 | Python 3.11.9 |
|---|---:|---:|
| WP11 handler/evaluator/condition focus | 158 passed | 158 passed |
| WP02–WP10 focus | 563 passed | 563 passed |
| all `tests/rules` | 618 passed | 618 passed |
| WP01 characterization | 25 passed | 25 passed |
| targeted runtime/role/Aspect/Career/determinism/snapshot | 47 passed | 47 passed |
| collection | 682 nodes | 682 identical nodes |
| node-ID SHA-256 (LF-joined IDs plus final LF) | `7696319e755464f53330398631f59d713d0500d2117dc897a47ecedffdc75ca1` | identical |
| complete suite run 1 | 680 passed, 2 skipped | 680 passed, 2 skipped |
| complete suite run 2 | 680 passed, 2 skipped | 680 passed, 2 skipped |
| Yoga normal/reverse/A/B/C | 5 passed each | 5 passed each |
| both loader orders | 2 passed each | 2 passed each |
| lint | five supported files once | same |
| strict snapshots 1/2 | match | match |

The linted files were exactly `derived_rules.yml`, `m1_rules.yaml`, `macros.yaml`, `primitives.yml`, and `yogas.yaml`.

## 17. Compatibility, snapshots, and artifacts

- Active Yoga, Career, runtime, loaders, rules/tables, public models/output, dependencies, CI, fixtures, and goldens were not edited.
- Legacy matched/unmatched characterization remains green and registry handler identities remain unchanged.
- No dormant Yoga helper was activated or removed; `HOUSE_LORDS_COMBINATION` remains unregistered/unknown.
- Approved snapshot and repository-root `-` remain SHA-256 `da2059ba3cfb92eed267f93d1e41585dac1422d68f685022c8609cfd04ad57af`.
- `tests/tmp_snapshot.json` remains SHA-256 `b31165671990beefca37557801f345ebe3e45e3e7e795e3f09884af99a4db257`.
- Scoped artifact status is empty. `git diff --check` exits zero; only inherited LF-to-CRLF warnings appear.

## 18. Deferred work and WP12 boundary

WP12 retains active loader/file-format validation. WP13 retains active Yoga migration/compatibility projection. WP16 retains remaining caller migration and removal of legacy adapters. No owner/SME decision was required because no protected astrology or public behavior changed.

WP12 was not started: no loader, condition format, YAML/rule file, source-location diagnostic, or active Yoga path was modified.

## 19. Final disposition

All six exposed registered IDs now execute through five pure prepared-state canonical handlers with WP05 validation, truthful WP06/WP07 availability, immutable WP02 results, WP03 serialization, WP09 alias/context/cache identity, and WP10 leaf/tree integration. Aspect representation/order/duplicates and ambiguous target behavior are preserved; functional roles use prepared facts only; exaltation preserves source/value meaning without doctrine; HOUSE_OCCUPANT remains separately identified. All required dual-lane, determinism, compatibility, lint, snapshot, and artifact gates are green.

**VERDICT: COMPLETE**

**WP12_READY: YES**

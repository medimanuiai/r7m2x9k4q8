# WP16 — Legacy Caller Migration and Adapter Retirement Completion Report

Date: 2026-07-16

**VERDICT: COMPLETE**

**WP17_READY: YES**

## 1. Scope and prerequisite gate

WP16 was executed as the bounded final caller/runtime cleanup after reviewing the locked Prompt-01 decisions and execution plan, Final Audit Consolidation, the final WP00-R and WP01 reports, WP02 through WP15 completion reports, and completed Reports/Audit 02, 03, 04, 07, 08, 10, 12, 14, 16, 17, 18, 19, 21, 22, and 25. Every predecessor records `VERDICT: COMPLETE`; WP15 records `WP16_READY: YES`.

The inherited dirty worktree contained the accumulated WP02-WP15 implementation, prompt moves, tracked edits, untracked typed modules/tests, and unrelated documentation state. It was recorded and preserved. No restore, staging, snapshot update, rule edit, dependency change, CI change, or unrelated overwrite was performed.

Before WP16 production edits, Python 3.14.6 and Python 3.11.9 each collected 757 identical nodes. Both repeated full runs in both lanes produced 755 passed and 2 optional skips. Yoga permutations, both loader trigger orders, five-file lint, and strict snapshot comparisons passed. The approved snapshot remained SHA-256 `da2059ba3cfb92eed267f93d1e41585dac1422d68f685022c8609cfd04ad57af`.

## 2. Authoritative live inventory and deviations

The pre-edit executable inventory classified the requested symbols as follows:

| Surface | Pre-edit classification | WP16 disposition |
|---|---|---|
| `in_sign`, `in_house`, `lord_of_house`, `is_exalted` | definitions/self-calls in `rules/runtime.py`; direct calls in `test_rule_runtime.py` | removed; canonical typed tests retain the supported facts and explicitly protect sign/exaltation non-equivalence |
| `evaluate_rule`, `evaluate_rule_with_score` | definitions/self-calls in `rules/runtime.py`; calls in two runtime tests and two tooling modules; negative strings in Career architecture tests | executable definitions/callers removed; negative strings retained only in architecture tests/report |
| `RuleMatch` | compatibility model in `engine/models.py`; constructed only by `rules/runtime.py` | removed because no independent consumer existed |
| runtime eager/lazy loading | import-time and call-time bootstrap in `rules/runtime.py` with broad exception swallowing | removed with the runtime module |
| `record_predicate`, `record_rule` | compatibility instrumentation definitions; runtime/coverage callers | instrumentation module and all side effects removed |
| `RULE_REGISTRY`, `load_rules_from_dir`, `get_rule` | generic loader plus supported Yoga-loader/test interoperability | retained independently; made deterministic and identity-preserving |
| Yoga loader/registry | supported compatibility loader API; typed Yoga execution itself was already independent | retained; both trigger orders characterized |
| `generate_full_artifacts.py` | direct generic-loader/runtime caller | migrated to typed Yoga and Career batches |
| `rule_coverage.py` | forced every generic row through runtime and read global instrumentation | migrated to explicit typed records with no global state |

The live inventory also found a material deviation from WP15's narrow starting expectation: `rules/engine.py` and `rules/predicates.py` remained an executable tuple-compatible direct-handler boundary with active characterization tests and registry bootstrap coupling. Keeping it would violate the required zero tuple/dictionary consumer and no direct registered-handler call invariants. WP16 therefore migrated those tests, pointed registry definitions directly at canonical typed handlers, made `PredicateEvaluator` the sole production invocation point, and removed both compatibility modules and `PredicateHandlerView`.

Documentation/history occurrences were not counted as callers. Same-name symbols outside the Parāśara rule subsystem were not changed.

## 3. Compatibility captures and tooling migration

Before migration, the golden fixture produced these temporary legacy-tool outputs identically in both lanes:

| Output | Bytes | SHA-256 |
|---|---:|---|
| legacy `rule_traces.json` | 14,400 | `5fed472d7d72a0e37b9106ef44fc5dd32045a39df2d31018555aa465a220c1e9` |
| legacy `career_rule_traces.json` | 7,283 | `99a5e7bbf811850e4ab78783ec83082155159ae2836d541ddbddf50322f4ed33` |
| legacy coverage JSON | 611 | `add66a4f15bbecf314387226110a36e8233169f5017918af4943b480f9f37238` |

No executable consumer of either trace artifact, `domain_prediction.json`, or the synthesized explainability artifact existed. WP16 retained the two trace filenames as one-way, detached logical projections from the exact semantic owners and removed the obsolete hand-built domain/explainability path. The new stable outputs are:

| Output | Typed owner/records | Bytes | SHA-256 |
|---|---|---:|---|
| `rule_traces.json` | three source-ordered WP13 Yoga records | 35,303 | `35d58eab17166e69e2a81205578bf08908d24d9e517ce88b79d2ec3a92b8fd9d` |
| `career_rule_traces.json` | four source-ordered WP15 Career candidates for the golden fixture | 38,791 | `00a67afca63e868354af064a3ad629b655ee662d5c1900119836594c60b8fe88` |
| coverage JSON | seven domain-qualified typed rule records and actually evaluated predicate records | 727 | `60c9e72b2d3b0c45056242df2fcf36e2a06d6d2fc98c9b85696173bb573f33ae` |

Coverage now reports numerator, denominator, ratio, stable domain-qualified ID/version, hit count, and factual/non-factual status counts. For the fixed fixture it inspects three Yoga records plus four Career candidates: 7/7, ratio 1.0. It no longer claims all 13 generic metadata rows were semantically evaluated. Skipped leaves without a typed result are not counted. The existing `rules`/`predicates` outer contract and `rules.hits` consumer path remain usable.

Tests prove deterministic bytes, cold/warm repetition, fresh-process/cross-version equality, CWD independence, detached projections, safe error projection, and no write to tracked/fixed artifacts.

## 4. Runtime and registry retirement

Removed executable compatibility files:

- `systems/Parasara/engine/rules/runtime.py`;
- `systems/Parasara/engine/rules/engine.py`;
- `systems/Parasara/engine/rules/predicates.py`;
- `tests/testing_framework/instrumentation.py`.

Removed compatibility-only `RuleMatch` from `engine/models.py`. Registry definitions now store the canonical WP08/WP11 typed handlers. `PredicateEvaluator` invokes `definition.handler` through one internal boundary after typed state/context checks and strict parameter validation. No production/test/tool caller imports the removed modules or calls a retired symbol.

The generic loader remains because `yoga_loader.py`, `test_yoga_loader.py`, and explicit interoperability tests use it independently. Its traversal now sorts directories/files, ignores non-mapping rows without losing later records, preserves the shared registry object's identity, and applies deterministic later-source precedence. Repository loading still yields 13 IDs and the same `yogas.yaml` winner for the duplicate `rajayoga_naive` ID. Generic-then-Yoga and Yoga-then-generic orders both pass.

## 5. Test migration mapping

| Retired assertion | Replacement/proof |
|---|---|
| raw sign/house/lord Boolean helpers | WP08/WP11 canonical predicate suites plus typed public-boundary smoke tests |
| sign-based exaltation helper | explicit test proves a planet sign is not reinterpreted as canonical `PLANET_EXALTED` |
| dictionary `evaluate_rule` match/mismatch | typed `PredicateResult` and recursive `ConditionResult` characterization |
| runtime registry merge/provenance | deterministic generic duplicate/source precedence, stable source identity, and both loader-order tests |
| eight-field compatibility result/cache tests | ten-field canonical result, instance cache, strict serialization, and safe typed status tests |
| direct handler view/scoped legacy evaluator tests | canonical registry handler identity, frozen isolated registry, and sole evaluator invocation tests |
| global instrumentation coverage smoke | exact typed coverage numerator/denominator/IDs/status/hash contract |

No astrology doctrine, rule content, weights, Career score/confidence/narrative, Yoga matching/order/projection, public schema, approved snapshot, dependency, or CI configuration changed.

## 6. Files attributable to WP16

Production/runtime:

- `systems/Parasara/engine/models.py`;
- `systems/Parasara/engine/rules/canonical.py`;
- `systems/Parasara/engine/rules/evaluator.py`;
- `systems/Parasara/engine/rules/loader.py`;
- `systems/Parasara/engine/rules/models.py`;
- `systems/Parasara/engine/rules/registry.py`;
- `systems/Parasara/engine/rules/yoga_loader.py`;
- deleted `engine.py`, `predicates.py`, and `runtime.py` under `engine/rules/`.

Tooling/tests:

- `tests/testing_framework/generate_full_artifacts.py`;
- `tests/testing_framework/rule_coverage.py`;
- `tests/testing_framework/coverage_engine.py`;
- new `tests/testing_framework/typed_rule_evaluation.py`;
- deleted `tests/testing_framework/instrumentation.py`;
- migrated predicate, condition, registry, Yoga, Career, and coverage characterization files;
- deleted `systems/Parasara/tests/test_rule_runtime.py` and `test_rule_runtime_merge.py`;
- new `systems/Parasara/tests/test_generic_rule_loader.py`;
- new `tests/test_wp16_typed_tooling.py`;
- new `tests/rules/test_wp16_legacy_retirement.py`;
- this report.

Inherited WP02-WP15 edits visible in the dirty tree are not claimed as WP16 work.

## 7. Final dual-lane evidence

| Gate | Python 3.14.6 | Python 3.11.9 |
|---|---:|---:|
| focused WP16 suite | 32 passed | 32 passed |
| final collection | 759 | 759 identical IDs |
| node-ID SHA-256 (LF joined plus final LF) | `167bf427a6a5e1ba1ffb18593202df64584787c13086152b7b40a18028b67598` | identical |
| full suite pass 1 | 757 passed, 2 skipped | 757 passed, 2 skipped |
| full suite pass 2 | 757 passed, 2 skipped | 757 passed, 2 skipped |
| Yoga normal/reverse/A/B/C | 5 passed in every order | 5 passed in every order |
| generic-loader trigger orders | 4 loader tests passed | 4 loader tests passed |
| rule lint | five supported files exactly once | identical |
| strict approved snapshot | matched twice | matched twice |

Two fresh processes per lane reproduced all three Career public hashes unchanged:

- golden: 403 bytes, `74442a0726173dcac3c521f1e67542443c16c43fbb39e7bded27f9e1601e3be3`;
- Surya test: 3,495 bytes, `fee279260217eabb6a0f037d48d306888571fdf4c1c259630eca4337b5df9974`;
- Surya generated: 584 bytes, `169cf5ce5ac9d8e678b160daf23293f365f2ab192a02a7aad90caab4da839dd9`.

The same processes reproduced the artifact and coverage hashes in Section 3 exactly. Approved snapshot and repository-root `-` remain `da2059ba...57af`; `tests/tmp_snapshot.json` remains `b3116567...257`. Tracked artifact/report files were not regenerated. `git diff --check` passes with inherited line-ending warnings only.

## 8. Architecture conclusion

Executable AST enforcement proves zero definitions/imports/references/callers for the retired raw helpers, rule evaluators, instrumentation functions, and compatibility result model; zero old runtime/engine/predicate modules; no tooling dependency on mutable registry warmth or global instrumentation; and no direct canonical handler invocation outside `PredicateEvaluator` and explicit handler unit tests. Yoga and Career continue through their typed batches and unchanged one-way public projections.

WP17 was not started.

**VERDICT: COMPLETE**

**WP17_READY: YES**

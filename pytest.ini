[pytest]
addopts = -q
# Enable xdist when available; not required but supported
# Usage: pytest -n auto
markers =
    integration: integration tests
    surya: tests requiring the optional Surya Siddhanta integration
filterwarnings =
    ignore::DeprecationWarning
